/**
 * @description Typically, square brackets  are used to clarify or add information to quotations.
 * @export
 */
export type SquareBracket =
  | '[' // Brackets (left)
  | ']' // Brackets (right)
;
