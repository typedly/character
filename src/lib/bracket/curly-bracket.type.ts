/**
 * @description Curly brackets, also known as braces, are rarely used punctuation marks that are used to group a set.
 * @export
 */
export type CurlyBracket =
  | '{' // Braces (left)
  | '}' // Braces (right)
;
